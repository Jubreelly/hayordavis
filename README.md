<!-- # HayorDavis-Stores (Frontend)

Simple static frontend for HayorDavis Stores — two shop locations (Kitchen & Home; Fashion & Appliances).
Built as static HTML/CSS/JS for easy GitHub Pages hosting.

## How to use
1. Place your product images inside the `Assets/` folder (names used in `script.js` are examples).
2. Edit `script.js` product arrays to change product titles, prices, stock, and image filenames.
3. Open `index.html` locally or host on GitHub Pages.

## Contact info (already embedded)
- Phone / WhatsApp: +234 703 667 9275
- Email: Temitopefausat76@gmail.com

## To publish
```bash
git init
git add .
git commit -m "Initial frontend for HayorDavis Stores"
# create repo on GitHub, then:
git remote add origin git@github.com:YOURUSERNAME/HayorDavis-Stores.git
git push -u origin main -->
